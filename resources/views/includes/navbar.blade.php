<nav>
    {{--
    <ul class="container">
        @foreach ($categories as $category)
            <li>
                <i class="fa-solid fa-{{ $category['icon'] }}"></i>
                <div>{{ $category['name'] }}</div>
            </li>
        @endforeach
    </ul>
    --}}
</nav>
